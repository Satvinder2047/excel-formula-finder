{
  "name": "excel-formula-finder",
  "version": "1.0.0",
  "description": "Search-based Excel formula reference. Looks formulas up live via web search + AI instead of static per-formula pages.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
  }
}
